---
description: 禁止助手在未获用户明示时自动执行 git 提交或推送
globs:
alwaysApply: true
---

## Git：不自动提交

- **除非用户在当前对话中明确要求**（例如明确说「提交」「commit」「push」「帮我 git」），否则**不要**执行 `git add`、`git commit`、`git push` 以及任何会创建提交或推送到远程的命令。
- 完成任务后，可**用文字**说明改了哪些文件、并**可选地**给出建议的 commit message；由用户在本地自行决定是否提交。
- 若仓库内其他规则（如 Linear、MCP、项目惯例）曾写「完成后提交」，**以本规则为准**：仍须用户先明确要求再执行提交类命令。
